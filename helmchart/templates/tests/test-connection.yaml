apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "world.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "world.name" . }}
    helm.sh/chart: {{ include "world.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "world.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
